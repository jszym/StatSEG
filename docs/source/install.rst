Installation
============

Installation of StatSEG is very simple.

1. Clone the Repository::

    git clone https://github.com/jszym/statseg

2. Install the Dependencies::

    cd statseg
    pip install -r requirements.txt

And voilà, StatSEG is installed.